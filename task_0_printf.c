#include "main.h"
/**
 * Printf - returns number of characters in a string
 * Return: 0
 * @format: pointer to a null terminated char
 */
int _printf(const char *format, ...);
{
	printf("%d", (printf("All cows eat grass")));
	return (0);
}
